spring:
  application:
    name: gateway
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: stock-service
          uri: lb://stock-service
          predicates:
            - Path=/gw/stock/**
          filters:
            - StripPrefix=2
  profiles:
    active: dev

server:
  port: 8999