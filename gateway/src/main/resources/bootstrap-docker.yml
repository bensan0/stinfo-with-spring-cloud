spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos.stinfo.orb.local
        namespace: stinfo
      #        username: nacos
      #        password: pass.123
      config:
        # 配置中心地址
        server-addr: nacos.stinfo.orb.local
        namespace: stinfo
#        username: nacos
#        password: pass.123