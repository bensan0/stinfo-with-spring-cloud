spring:
  application:
    name: auth-service
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yml
        shared-configs:
          - application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
server:
  port: 8086